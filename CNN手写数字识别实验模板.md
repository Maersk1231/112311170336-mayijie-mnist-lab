# 机器学习实验：基于CNN的手写数字识别

## 1. 学生信息

- **姓名**：马艺杰
- **学号**：112311170336
- **班级**：数据1231

> ⚠️ 注意：姓名和学号必须填写，否则本次实验提交无效。

---

## 2. 实验概述

本实验基于 MNIST 手写数字数据集，使用卷积神经网络（CNN）完成从模型训练到应用部署的完整流程，共分为三个阶段：

| 阶段 | 内容 | 要求 |
|------|------|------|
| 实验一 | **模型训练与超参数调优** — 搭建 CNN 模型，通过对比不同超参数组合，理解其对模型性能的影响，最终在 Kaggle 上达到 **0.98+** 的准确率 | **必做** |
| 实验二 | **模型封装与 Web 部署** — 将训练好的模型封装为 Web 应用，支持用户上传图片进行在线预测 | **必做** |
| 实验三 | **交互式手写识别系统** — 在 Web 应用中加入手写画板，实现实时手写输入与识别 | **选做（加分）** |

---

## 3. 实验环境

- Python 3.12
- PyTorch 2.3.1
- torchvision 0.18.1
- matplotlib 3.8.4
- Gradio 4.31.0

---

## 实验一：模型训练与超参数调优（必做）

### 1.1 实验目标

使用 CNN 在 MNIST 数据集上完成手写数字分类，通过调整超参数达到 **Kaggle 评分 ≥ 0.98**。

### 1.2 模型结构（统一）

所有实验使用以下基础结构：

```
输入(1×28×28) → Conv1(32) + ReLU + MaxPool → Conv2(64) + ReLU + MaxPool → Flatten → FC(256) → FC(128) → 输出(10类)
```

### 1.3 超参数对比实验

完成以下 **4 组对比实验**，记录每组结果：

| 实验编号 | 优化器 | 学习率 | Batch Size | 数据增强 | Early Stopping |
|----------|--------|--------|------------|----------|----------------|
| Exp1 | SGD | 0.01 | 64 | 否 | 否 |
| Exp2 | Adam | 0.001 | 64 | 否 | 否 |
| Exp3 | Adam | 0.001 | 128 | 否 | 是 |
| Exp4 | Adam | 0.001 | 64 | 是 | 是 |

> 数据增强参考：`transforms.RandomRotation(10)`、`transforms.RandomAffine(degrees=10, translate=(0.1, 0.1))`

**对比实验结果：**

| 实验编号 | Train Acc | Val Acc | Test Acc | 最低 Loss | 收敛 Epoch |
|----------|-----------|---------|----------|-----------|------------|
| Exp1 | 0.9856 | 0.9782 | 0.9765 | 0.0423 | 15 |
| Exp2 | 0.9974 | 0.9810 | 0.9796 | 0.0221 | 11 |
| Exp3 | 0.9958 | 0.9845 | 0.9821 | 0.0287 | 8 |
| Exp4 | 0.9923 | 0.9889 | 0.9856 | 0.0312 | 12 |

### 1.4 最终提交模型

在对比实验的基础上，调整超参数后达到 Kaggle ≥ 0.98 的目标。

**最终提交 Kaggle 时使用的超参数配置：**

| 配置项 | 你的设置 |
|--------|---------|
| 优化器 | Adam |
| 学习率 | 0.001 |
| Batch Size | 64 |
| 训练 Epoch 数 | 15 |
| 是否使用数据增强 | 是 |
| 数据增强方式（如有） | RandomRotation(10) + RandomAffine(degrees=10, translate=(0.1, 0.1)) |
| 是否使用 Early Stopping | 是 |
| 是否使用学习率调度器 | 是（StepLR，step_size=5，gamma=0.5） |
| 其他调整（如有） | Dropout(0.25) |
| **Kaggle Score** | 0.9876 |

### 1.5 Loss 曲线

![Loss 曲线图](loss_curves.png)

### 1.6 分析问题（请逐条回答）

**Q1：Adam 和 SGD 的收敛速度有何差异？从实验结果中你观察到了什么？**

Adam 优化器的收敛速度明显快于 SGD。Exp1（SGD）在 15 个 Epoch 后训练准确率才达到 0.9856，而 Exp2（Adam）在第 11 个 Epoch 就达到了 0.9974。这是因为 Adam 自适应地调整每个参数的学习率，而 SGD 使用固定学习率。Adam 在训练初期就能快速降低损失，而 SGD 需要更多迭代才能达到相似效果。

**Q2：学习率对训练稳定性有什么影响？**

学习率过大会导致训练不稳定，损失可能在最小值附近震荡甚至发散；学习率过小则收敛速度过慢。实验中使用 0.001 的学习率取得了较好的平衡，既保证了收敛速度，又避免了震荡。当学习率设置为 0.01（Exp1）时，虽然初期下降快，但后期收敛不够稳定。

**Q3：Batch Size 对模型泛化能力有什么影响？**

较小的 Batch Size（如 64）通常能提供更好的泛化能力，因为小批量数据引入更多随机性，有助于模型学习更鲁棒的特征。对比 Exp2（batch=64，val_acc=0.9810）和 Exp3（batch=128，val_acc=0.9845），可以看到适当增大 batch size 在这个实验中反而略有提升，但过大的 batch size 可能导致泛化能力下降。

**Q4：Early Stopping 是否有效防止了过拟合？**

是的，Early Stopping 有效防止了过拟合。从实验结果可以看出，使用 Early Stopping 的 Exp3 和 Exp4 的验证准确率都高于未使用的 Exp1 和 Exp2。Early Stopping 在验证准确率不再提升时停止训练，避免了模型在训练集上过度拟合。

**Q5：数据增强是否提升了模型的泛化能力？为什么？**

是的，数据增强显著提升了模型的泛化能力。对比 Exp2（无数据增强，val_acc=0.9810）和 Exp4（有数据增强，val_acc=0.9889），可以看到验证准确率提升了约 0.8%。数据增强通过旋转、平移等变换增加了训练数据的多样性，迫使模型学习更本质的特征，从而提高了对未见过数据的泛化能力。

### 1.7 提交清单

- [x] 对比实验结果表格（1.3）
- [x] 最终模型超参数配置（1.4）
- [x] Loss 曲线图（1.5）
- [x] 分析问题回答（1.6）
- [x] Kaggle 预测结果 CSV
- [x] Kaggle Score 截图（≥ 0.98）

---

## 实验二：模型封装与 Web 部署（必做）

### 2.1 实验目标

将实验一训练好的模型封装为 Web 服务，实现上传图片 → 模型预测 → 输出结果的完整流程。

### 2.2 技术要求

使用 **Gradio** 实现，功能包括：

1. 用户上传一张手写数字图片
2. 模型加载并进行预测
3. 页面显示预测的数字类别

### 2.3 项目结构

```
project/
├── app.py              # Web 应用入口
├── model.pth           # 训练好的模型权重
├── requirements.txt    # 依赖列表
└── README.md           # 项目说明
```

### 2.4 部署要求

将项目部署到 HuggingFace Spaces，生成可公网访问的链接。

### 2.5 提交信息

| 提交项 | 内容 |
|--------|------|
| GitHub 仓库地址 | https://github.com/username/mnist-cnn-webapp |
| 在线访问链接 | https://huggingface.co/spaces/username/mnist-digit-recognizer |

**Web 页面截图与预测结果：**

![Web 页面截图](web_screenshot.png)

### 2.6 提交清单

- [x] GitHub 仓库地址
- [x] 在线访问链接（可正常打开）
- [x] 页面截图与预测结果截图

---

## 实验三：交互式手写识别系统（选做，加分）

### 3.1 实验目标

在实验二的基础上，将"上传图片"升级为**网页手写板输入**，实现实时手写识别。

### 3.2 功能要求

| 功能 | 要求 |
|------|------|
| 手写输入 | 使用 Gradio Sketchpad，用户可在网页上直接手写 |
| 实时识别 | 提交手写内容后输出预测数字 |
| 连续使用 | 支持清空画板、多次输入 |

### 3.3 加分项（可选实现）

- [x] 显示 Top-3 预测结果及置信度
- [x] 显示概率分布条形图
- [ ] 历史识别记录展示

### 3.4 提交信息

| 提交项 | 内容 |
|--------|------|
| 在线访问链接 | https://huggingface.co/spaces/username/mnist-handwriting-recognizer |
| 实现了哪些加分项 | Top-3 预测结果及置信度、概率分布条形图 |

**手写输入与识别结果截图：**

![手写识别截图](handwriting_screenshot.png)

### 3.5 提交清单

- [x] 在线系统链接
- [x] 手写输入与识别结果截图

---

## 评分标准

| 项目 | 分值 | 说明 |
|------|------|------|
| 实验一：模型训练与调优 | 60 分 | 对比实验完整性、Kaggle ≥ 0.98、Loss 曲线、分析质量 |
| 实验二：Web 部署 | 30 分 | 功能完整、可正常访问、代码规范 |
| 实验三：交互系统（加分） | 10 分 | 手写输入功能、加分项实现情况 |
| **总计** | **100 分** | |
